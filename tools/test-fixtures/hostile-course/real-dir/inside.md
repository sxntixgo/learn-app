---
title: Inside
---

The target of `linkdir`. Reaching this file through the `linkdir` symlink must
be refused even though the target itself is inside the course directory —
"refuse symlinks" is not conditional on where the link happens to point today.
